{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-outline-success btn-lg border border-dark rounded-lg mt-3">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
